# API Configuration
# Local dev:
VITE_API_URL=http://localhost:5007/api
# Production (VPS): set in .env.production (gitignored) or the host env var, e.g.
# VITE_API_URL=https://api.footmad.online/api
